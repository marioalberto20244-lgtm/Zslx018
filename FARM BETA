-- ==============================================================================
-- Z-HUB: MINI HUB v44.1 (AUTO-RECOVERY / CAR FIX)
-- ==============================================================================
local p = game.Players.LocalPlayer
local rs = game:GetService("RunService")

local char, hum, hrp
local miAsiento = nil -- Variable persistente fuera del loop

local function refreshVars()
    char = p.Character or p.CharacterAdded:Wait()
    hum = char:WaitForChild("Humanoid")
    hrp = char:WaitForChild("HumanoidRootPart")
end
refreshVars()

-- 1. UI (Igual que el tuy6)
local sg = Instance.new("ScreenGui", p.PlayerGui); sg.Name = "Z_Mini_Hub"; sg.ResetOnSpawn = false
local m = Instance.new("Frame", sg); m.Size, m.Position, m.BackgroundColor3, m.Active, m.Draggable = UDim2.new(0,180,0,60), UDim2.new(0.05,0,0.3,0), Color3.fromRGB(15,15,15), true, true
local b = Instance.new("TextButton", m); b.Size, b.Position, b.Text, b.TextColor3, b.Font = UDim2.new(1,-10,1,-10), UDim2.new(0,5,0,5), "AUTO-FARM: OFF", Color3.new(1,1,1), Enum.Font.Code; b.BackgroundColor3 = Color3.fromRGB(80,0,0)

local items = {["Fake Designer Bag"]=3, ["Fake Watch"]=5, ["Sarsaparilla"]=1}

local function getQty(name)
    local c, s = 0, name:lower()
    for _, v in pairs(p.Backpack:GetChildren()) do if v.Name:lower():find(s) then c+=1 end end
    local t = char:FindFirstChildWhichIsA("Tool"); if t and t.Name:lower():find(s) then c+=1 end
    return c
end

local noclipLoop = nil
local function toggleNoclip(state)
    if state then
        if not noclipLoop then
            noclipLoop = rs.Stepped:Connect(function()
                if not char then return end
                for _, v in pairs(char:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = false end end
            end)
        end
    else
        if noclipLoop then noclipLoop:Disconnect(); noclipLoop = nil end
    end
end

local function focus(targetPos)
    hrp.CFrame = CFrame.new(hrp.Position, Vector3.new(targetPos.X, hrp.Position.Y, targetPos.Z))
end

-- MOTOR TP VEHICLE (FIXED)
local function safeVehicleTP(dest, autoDismount, seat)
    if not seat or not seat.Parent then return end -- Si no hay asiento, NO HACE TP (Evita confusiones)
    local veh = seat:FindFirstAncestorOfClass("Model")
    if veh then
        for _, part in pairs(veh:GetDescendants()) do 
            if part:IsA("BasePart") then part.Anchored = true; part.AssemblyLinearVelocity = Vector3.zero end 
        end
        local _, sz = veh:GetBoundingBox()
        veh:PivotTo(CFrame.new(dest.X, dest.Y + (sz.Y/2) + 0.3, dest.Z) * CFrame.Angles(0, math.rad(3), 0))
        task.wait(0.3) 
        for _, part in pairs(veh:GetDescendants()) do if part:IsA("BasePart") then part.Anchored = false end end
    end
    if autoDismount then
        hum.Sit = false; task.wait(0.1)
        hrp.Anchored = true
        hrp.CFrame = CFrame.new(dest.X + 8, dest.Y + 2, dest.Z)
        task.wait(0.1)
        hrp.Anchored = false
    end
end

local function AutoBuy(segundos)
    local endTime = tick() + segundos
    while tick() < endTime do
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("ProximityPrompt") then
                local pPart = obj.Parent:IsA("BasePart") and obj.Parent or obj.Parent.Parent
                if pPart and pPart:IsA("BasePart") and (pPart.Position - hrp.Position).Magnitude <= 18 then
                    local txt = (obj.ObjectText .. obj.ActionText):lower()
                    for item, maxQty in pairs(items) do
                        if txt:find(item:lower()) and getQty(item) < maxQty then
                            focus(pPart.Position)
                            obj.HoldDuration = 0; fireproximityprompt(obj); task.wait(0.05)
                        end
                    end
                end
            end
        end
        task.wait()
    end
end

local function AutoInteract(keywords, waitAfter)
    local startTime = tick()
    while tick() - startTime < 4 do
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("ProximityPrompt") then
                local pPart = obj.Parent:IsA("BasePart") and obj.Parent or obj.Parent.Parent
                if pPart and pPart:IsA("BasePart") and (pPart.Position - hrp.Position).Magnitude <= 18 then
                    local txt = (obj.ObjectText .. obj.ActionText):lower()
                    for _, kw in pairs(keywords) do
                        if txt:find(kw) then
                            focus(pPart.Position)
                            local interactEnd = tick() + waitAfter
                            while tick() < interactEnd do
                                obj.HoldDuration = 0; fireproximityprompt(obj); task.wait(0.1) 
                            end
                            return true
                        end
                    end
                end
            end
        end
        task.wait(0.2)
    end
end

-- LOOP MAESTRO FIX
local autoFarm = false
b.Activated:Connect(function()
    autoFarm = not autoFarm
    b.Text = autoFarm and "AUTO-FARM: ON" or "AUTO-FARM: OFF"
    b.BackgroundColor3 = autoFarm and Color3.fromRGB(0, 100, 0) or Color3.fromRGB(80, 0, 0)
end)

task.spawn(function()
    while true do
        task.wait(0.5)
        if autoFarm then
            pcall(function()
                refreshVars()
                
                -- SI NO TENEMOS CARRO, BUSCARLO (O ir al inicio)
                if not miAsiento or not miAsiento.Parent then
                    if hum.SeatPart then 
                        miAsiento = hum.SeatPart -- Guardar el nuevo carro
                    else
                        -- Teletransportar al punto de inicio para buscar carro
                        hrp.CFrame = CFrame.new(-1947, 3, 55)
                        task.wait(1)
                        return -- Reiniciar loop para detectar carro
                    end
                end
                
                -- ASEGURAR ESTAR SENTADO ANTES DE CUALQUER FASE
                if not hum.Sit then
                    hrp.CFrame = miAsiento.CFrame
                    task.wait(0.2)
                    miAsiento:Sit(hum)
                    task.wait(0.3)
                end

                -- FASE 2: MERCADO
                safeVehicleTP(Vector3.new(-1821, 2, 15), true, miAsiento)
                toggleNoclip(true)
                hrp.CFrame = CFrame.new(-1843, 3, 8); task.wait(0.2); AutoBuy(3) 
                hrp.CFrame = CFrame.new(-1831, 3, 11); task.wait(0.2); AutoBuy(0.2) 
                
                -- REGRESO AL CARRO (FIX: TP AL ASIENTO)
                hrp.CFrame = miAsiento.CFrame; task.wait(0.1); toggleNoclip(false)
                local int1 = 0
                while hum.SeatPart ~= miAsiento and int1 < 10 do miAsiento:Sit(hum); task.wait(0.3); int1 += 1 end

                -- FASE 6: ENTREGA
                safeVehicleTP(Vector3.new(3486, 2, -761), true, miAsiento) 
                toggleNoclip(true)
                hrp.CFrame = CFrame.new(3500, 2, -752); task.wait(0.2)
                AutoInteract({"sell", "smuggle"}, 1) 

                -- REGRESO AL CARRO
                hrp.CFrame = miAsiento.CFrame; task.wait(0.1); toggleNoclip(false)
                local int2 = 0
                while hum.SeatPart ~= miAsiento and int2 < 10 do miAsiento:Sit(hum); task.wait(0.3); int2 += 1 end

                -- FASE 10: LAVADO
                safeVehicleTP(Vector3.new(-1872, 2, -6), true, miAsiento)
                toggleNoclip(true)
                hrp.CFrame = CFrame.new(-1847, 3, -9); task.wait(0.2)
                AutoInteract({"laundry", "dirty"}, 1) 

                -- FINAL: REGRESO AL CARRO
                hrp.CFrame = miAsiento.CFrame; task.wait(0.1); toggleNoclip(false)
                local int3 = 0
                while hum.SeatPart ~= miAsiento and int3 < 10 do miAsiento:Sit(hum); task.wait(0.3); int3 += 1 end
                
                task.wait(0.5)
            end)
        end
    end
end)
